{
  "name": "slack-demo",
  "scripts": {
    "dev": "next",
    "test": "echo 'test'",
    "build": "next build",
    "travis": "rm -rf dist && mkdir -p dist && zip -r dist/server.zip . -x@exclude.lst",
    "start": "next start"
  },
  "dependencies": {
    "next": "^7.0.2",
    "react": "^16.6.1",
    "react-dom": "^16.6.1"
  }
}
